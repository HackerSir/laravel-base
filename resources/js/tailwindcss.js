import * as tailwindComponents from 'vue-tailwind/dist/components';

const {
    // Some Tailwind Components Name, e.g. TTag
    ...defaultComponents
} = tailwindComponents;
const components = {
    // Some Tailwind Components Config
    ...defaultComponents
};

export default components;
